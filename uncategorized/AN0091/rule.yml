title: Hidden File or Directory Attribute Setting (AN0091-A)
id: 85f45ec6-f172-46d1-8ca8-78e93e6d6d20
status: experimental
description: Detects adversarial use of attrib.exe or PowerShell to set hidden and system attributes on files or directories, concealing malicious artifacts from user and administrator inspection.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_attrib:
        - Image|endswith: '\attrib.exe'
        - OriginalFileName: 'attrib.exe'
    selection_attrib_args:
        CommandLine|contains:
            - ' +H '
            - ' +h '
            - ' +S '
            - ' +s '
            - '+H+S'
            - '+hs'
    selection_powershell:
        - Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_powershell_args:
        CommandLine|contains:
            - 'Hidden'
            - 'System'
            - 'SetAttributes'
            - '[System.IO.File]::SetAttributes'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: ((selection_attrib and selection_attrib_args) or (selection_powershell and selection_powershell_args)) and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - System utilities or software installers that legitimately hide configuration directories (e.g., git, VS Code, or language runtime installers creating .dotfiles).
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.001
    - analytic.an0091
    - detection.det0032
---
title: Alternate Data Stream (ADS) File Creation (AN0091-B)
id: 28f1115f-02af-4bbf-b2ec-e80b9579cee0
status: experimental
description: Detects the creation of Alternate Data Streams (ADS) containing executable files or scripts, which can be used by adversaries to hide malicious payloads.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: file_event
    product: windows
    # Relies on Sysmon Event ID 11
detection:
    selection_ads:
        TargetFilename|contains:
            - '.exe:'
            - '.dll:'
            - '.bat:'
            - '.cmd:'
            - '.vbs:'
            - '.vbe:'
            - '.js:'
            - '.jse:'
            - '.ps1:'
            - '.lnk:'
            - '.sct:'
            - '.hta:'
            - ':$DATA'
    filter_zone:
        TargetFilename|contains: ':Zone.Identifier'
    condition: selection_ads and not filter_zone
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - Legitimate software installers or applications using ADS for database storage or metadata (excluding Zone.Identifier).
level: high
tags:
    - attack.defense-evasion
    - attack.t1564.001
    - analytic.an0091
    - detection.det0032
